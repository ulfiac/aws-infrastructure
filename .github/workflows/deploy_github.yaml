name: deploy github
run-name: "${{ inputs.terragrunt_action }}: github, ${{ inputs.type }}, ${{ inputs.name }}"

on:
  workflow_dispatch:
    inputs:
      type:
        default: all
        options:
          - all
          - repo
        required: true
        type: choice
      name:
        default: all
        required: true
        type: string
      terragrunt_action:
        default: plan
        options:
          - plan
          - plan destroy
          - apply
          - destroy
        required: true
        type: choice
      annotate_context_details:
        default: false
        required: false
        type: boolean
      terragrunt_parallelism:
        default: true
        required: false
        type: boolean
      terragrunt_strict_mode:
        default: true
        required: false
        type: boolean

permissions:
  contents: read
  id-token: write

jobs:
  deploy-github:
    name: ${{ inputs.name == 'all' && format('github/{0}', matrix.type) || format('github/{0}/{1}', matrix.type, inputs.name) }}
    strategy:
      matrix:
        type: ${{ fromJson(inputs.type == 'all' && '["repo"]' || format('["{0}"]', inputs.type)) }}
    uses: ./.github/workflows/reusable_terragrunt_action.yaml
    with:
      annotate_context_details: ${{ inputs.annotate_context_details }}
      aws_region: us-east-2
      github_environment: github
      terragrunt_action: ${{ inputs.terragrunt_action }}
      terragrunt_parallelism: ${{ inputs.terragrunt_parallelism }}
      terragrunt_queue_include_dir: ${{ inputs.name == 'all' && './**' || format('./{0}', inputs.name) }}
      terragrunt_strict_mode: ${{ inputs.terragrunt_strict_mode }}
      working_dir: "./github/${{ matrix.type }}"
    secrets: inherit
